import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  button: {
    marginTop: 16,
    padding: 12,
    backgroundColor: '#008970',
    borderRadius: 8,
    alignItems: 'center',
  },
  buttonText: {
    color: '#fff',
    fontSize: 16,
    fontWeight: 'bold',
  },
});
